# mamkilover
Telegram bot that cleans up URLs in your messages. It removes tracking links and query parameters,
and replaces links to some IV-unfriendly services with proxy services
that provide a better embedding and viewing experience.

